# Helm chart metadata

apiVersion: v2
name: pindrop
description: |
  Helm chart for Pindrop Travel Application.
  Deploys frontend (React/nginx) and backend (Node.js/Express) to Kubernetes.
type: application

# Chart version (template/config changes)
version: 0.1.0

# App version shown in Helm metadata
appVersion: "1.0.0"

keywords:
  - pindrop
  - travel
  - nodejs
  - react

maintainers:
  - name: Mallika Kulkarni
    email: mallika.kulkarni20@gmail.com

home: https://github.com/mallikakulkarni/pindrop

# icon: https://example.com/icon.png
