# Non-secret runtime config

apiVersion: v1
kind: ConfigMap
metadata:
  name: pindrop-config
  namespace: pindrop
  labels:
    app: pindrop
data:
  # Backend settings
  NODE_ENV: "production"
  PORT: "3001"
  DB_SSL: "false"
  # Frontend build-time reference value
  VITE_API_URL: "/api"
